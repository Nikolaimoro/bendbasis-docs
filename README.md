# bendbasis docs

Mintlify documentation for the bendbasis API.

## Development

Install the [Mintlify CLI](https://www.npmjs.com/package/mint):

```
npm i -g mint
```

Run the preview from the repository root:

```
mint dev
```

The preview runs at `http://localhost:3000`.
